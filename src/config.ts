export const APP_CONFIG = {
    // Destination name displayed in weather widgets and headers
    DESTINATION: 'ChiangMai',

    // Base currency for expense tracking (e.g., 'JPY', 'USD', 'EUR')
    BASE_CURRENCY: 'THB',

    // Visual symbol for the base currency
    CURRENCY_SYMBOL: '฿',

    // Display string for the exchange rate pair (e.g., 'JPY / USD')
    EXCHANGE_PAIR: 'THB / USD',

    // Mock exchange rate value for display
    MOCK_EXCHANGE_RATE: 35.50
};
